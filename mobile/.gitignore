node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Native projects are generated by `npx expo prebuild` / EAS — not checked in for the managed workflow
/ios/
/android/

# Expo / Metro caches
.expo-shared/
